{
  "name": "tickets-backend",
  "version": "1.0.0",
  "description": "Backend serverless para portal de seguimiento de incidencias",
  "main": "index.js",
  "scripts": {
    "compile": "tsc",
    "prebuild": "npm run compile && node -e \"require('fs').copyFileSync('package.json', 'dist/package.json')\"",
    "build": "sam build",
    "deploy": "sam deploy",
    "local": "sam local start-api",
    "test": "jest"
  },
  "keywords": [
    "aws",
    "serverless",
    "lambda",
    "dynamodb"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.948.0",
    "@aws-sdk/lib-dynamodb": "^3.490.0",
    "@aws-sdk/util-dynamodb": "^3.948.0",
    "uuid": "^13.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.131",
    "@types/node": "^20.10.6",
    "typescript": "^5.3.3"
  }
}
